WEB_BIND_HOST=0.0.0.0
WEB_PORT=80

SERVER_MODE=release
SERVER_FRONTEND_URL=https://nemotoken.online
RUN_MODE=standard
TZ=Asia/Hong_Kong

POSTGRES_USER=nemotoken
POSTGRES_PASSWORD=change-me-postgres
POSTGRES_DB=nemotoken

REDIS_PASSWORD=
REDIS_DB=0

ADMIN_EMAIL=admin@nemotoken.online
ADMIN_PASSWORD=change-me-admin-password

# Generate with: openssl rand -hex 32
JWT_SECRET=change-me-jwt-secret

# Generate with: openssl rand -hex 32
TOTP_ENCRYPTION_KEY=change-me-totp-key
